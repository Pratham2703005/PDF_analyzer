services:
  ministack:
    image: ministackorg/ministack:latest
    container_name: pdf-analyzer-ministack
    ports:
      - "4566:4566"
    environment:
      - LAMBDA_EXECUTOR=docker
      - LOG_LEVEL=INFO
    volumes:
      - "//var/run/docker.sock:/var/run/docker.sock"
